@model IEnumerable<Blog_final_Asp.Models.Autor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Login)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Login)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDautor }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDautor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDautor })
        </td>
    </tr>
}

</table>
